Option Explicit

Private Sub Workbook_Open()
    On Error Resume Next
    'Shortcut + sheets init
    SetupAutomationPack
    SetupKeyboardShortcuts
End Sub

Private Sub Workbook_BeforeClose(Cancel As Boolean)
    On Error Resume Next
    ClearKeyboardShortcuts
End Sub
