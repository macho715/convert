# OFFICIAL DOCS

공문서(official-doc-drafter) 작업 전용 폴더.

- **작업 위치**: `/docstyle-researcher` 및 `official-doc-drafter` 스킬의 모든 실행(스크립트·content.json·템플릿·산출 .docx)은 **이 폴더**에서만 수행한다.
- **산출물**: `out/` 또는 `output/` 하위에 `.docx` 저장. 템플릿·content.json은 이 폴더 또는 하위에 둔다.
